#include "html.h"

namespace html {

TAG_DECL(HTML);
TAG_DECL(HEAD);
TAG_DECL(STYLE);
TAG_DECL(SCRIPT);
TAG_DECL(BODY);
TAG_DECL(FORM);
TAG_DECL(DIV);
TAG_DECL(A);

ARGUMENT_DECL(src);
ARGUMENT_DECL(name);
ARGUMENT_DECL(type);

}  // namespace html
